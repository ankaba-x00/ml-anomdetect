services:
  api:
    build: .
    container_name: anomaly_api
    restart: always
    ports:
      - "7134:7134"
    env_file:
      - .env
    networks:
      - mlnet

networks:
  mlnet:
    driver: bridge
